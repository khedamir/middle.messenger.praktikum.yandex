{
  "extends": "stylelint-config-standard-scss",
  "rules": {
    "selector-class-pattern": [
      "^([a-z]+(-[a-z]+)?(__[a-z]+(-[a-z]+)?)?(_[a-z]+(-[a-z]+)?)?)$",
      {
        "message": "Селектор не соответствует соглашению по именованию БЭМ"
      }
    ]
  }
}
